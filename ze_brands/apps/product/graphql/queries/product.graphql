query productQuery($productId:Int){
  product(id:$productId){
    totalCount
    edges{
      node{
        id
        originalId
        sku
        name
        price
        brand{
          id
          originalId
          name
          description
        }
      }
    }
  }
}
